import 'package:flutter/material.dart';

/// App color palette extracted from design
class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF4DC3B3);
  static const Color primaryLight = Color(0xFFE0F5F3);
  static const Color primaryDark = Color(0xFF2DA89A);

  // Background Colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color cardBackground = Color(0xFFE0F5F3);
  static const Color white = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFF9E9E9E);

  // Accent Colors
  static const Color error = Color(0xFFFF5252);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);

  // Icon Colors
  static const Color iconGray = Color(0xFFBDBDBD);
  static const Color iconActive = primary;

  // Divider
  static const Color divider = Color(0xFFE0E0E0);

  // Disabled
  static const Color disabled = Color(0xFFE0E0E0);
}
